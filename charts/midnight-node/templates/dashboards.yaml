apiVersion: v1
data:
{{ (.Files.Glob "custom-dashboards/*.json").AsConfig | indent 2 }}
kind: ConfigMap
metadata:
  name: {{ include "midnight-node.fullname" . }}-dashboard
  labels:
    {{- include "midnight-node.labels" . | nindent 4 }}
    grafana_dashboard: "1"
