apiVersion: v1
kind: Service
metadata:
  name: {{ include "cardano-db-sync.fullname" . }}
  labels:
    {{- include "cardano-db-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cardano-db-sync.selectorLabels" . | nindent 4 }}
